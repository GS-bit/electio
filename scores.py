def get_candidates_by_score(database: Database) -> list:
    """
    It returns a list of the candidates considering the score.

    Arguments:
        database: the database

    Returns:
        a list in which the first element is the candidate with the greatest score, the second element is the
        candidate with the second greatest score and so on. The candidates are in the dictionary format, containing the keys "name", "age", "sex", "current_period", "indication", "email", "phone", "github",
        "linkedin", "personal_description", "qualities", "defects", "why_wants_to_work", "front_end_score",
        "back_end_score", "english_score", "proactivity_score", "resilience_score", "communicative_skills_score",
        "group_work_score" and "schedules".
    """

    pass
